#
# For a description of the syntax of this configuration file,
# see the file kconfig-language.txt in the NuttX tools repository.
#

menuconfig EXAMPLES_MUSICDEMO
	tristate "MUSIC DEMO"
	default y
	depends on GRAPHICS_LVGL
	---help---
		Enable build the Light and Versatile Graphics Library Demo programs

if EXAMPLES_MUSICDEMO

config EXAMPLES_MUSICDEMO_PRIORITY
	int "musicdemo task priority"
	default 100

config EXAMPLES_MUSICDEMO_STACKSIZE
	int "musicdemo stack size"
	default 16384

config EXAMPLES_MUSICDEMO_INPUT_DEVPATH
	string "Touchscreen device path"
	default "/dev/input0"
	depends on INPUT_TOUCHSCREEN
	---help---
		The path to the touchscreen device. Default: "/dev/input0"

endif # EXAMPLES_MUSICDEMO 
